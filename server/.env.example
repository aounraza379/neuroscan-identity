# NeuroSignature Backend Configuration
PORT=3001
CORS_ORIGIN=http://localhost:5173

# Master signing key — generate a strong random string
# openssl rand -hex 32
MASTER_SECRET=replace_me_with_a_64_char_hex_string

# Session settings
SESSION_TTL_MS=600000
TOKEN_EXPIRY_MS=30000
MAX_VERIFY_ATTEMPTS=5
RATE_LIMIT_WINDOW_MS=60000
